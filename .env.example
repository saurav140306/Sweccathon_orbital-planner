# Mesocosm AI (Ollama local — matches `mesocosm run local`)
MESOCOSM_MODEL=ollama/llama3.2
MESOCOSM_API_BASE=http://localhost:11434/v1
MESOCOSM_API_KEY=mesocosm

# Optional: treat Mesocosm as available without health check (dev only)
# MESOCOSM_FORCE_AVAILABLE=1
